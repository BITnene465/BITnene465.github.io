function About() {
  return (
    <section className="space-y-4">
      <p className="text-xs uppercase tracking-[0.4em] text-primary">step two</p>
      <h1 className="text-3xl font-semibold">About placeholder</h1>
      <p className="text-text-muted">
        Future iterations will pull structured bio, values, and timeline details from the profile data
        source defined in step four.
      </p>
    </section>
  )
}

export default About
