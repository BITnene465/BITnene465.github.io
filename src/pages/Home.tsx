function Home() {
  return (
    <section className="space-y-4">
      <p className="text-xs uppercase tracking-[0.4em] text-accent">step two</p>
      <h1 className="text-3xl font-semibold">Home placeholder</h1>
      <p className="text-text-muted">
        Layout and routing are online. Future steps will populate this page with hero, quick links, and
        recent activity modules.
      </p>
    </section>
  )
}

export default Home
